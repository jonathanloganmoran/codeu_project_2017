/**
 * CodeU Project Group 34
 * Skeleton connector .java class
 */

package codeu.desktop.gui;

import java.awt.Image;
import java.io.FileInputStream;
import java.net.URL;
import java.util.ResourceBundle;
import java.util.function.Supplier;
import java.util.logging.Logger;
import static javafx.application.ConditionalFeature.FXML;
import javafx.fxml.Initializable;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.scene.control.Label;
import javafx.scene.image.ImageView;

import com.jfoenix.controls._;


/**
 * FXML Controller class
 *
 * @author jonathanmoran
 */
public class NaviBarController implements Initializable {

    @FXML
    private JFXListView<Label> listView;
    
 /*   @Override
    public void initialize(URL url, ResourceBundle rb) {
  *    try {
  *        Label Conversations = new Label;
  *        Conversations.setGraphic(new ImageView(new Image(new FileInputStream("conversations icon source"))));
  *        listView.getItems().add(Conversations);
  */         
         /*   Label Users = new Label;
           * Users.setGraphic(new ImageView(new Image(new FileInputStream("users icon src")));
           * listView.getItems().add(Users);
           * 
           * Label Preferences = new Label;
           * Preferences.setGraphic(new ImageView(new Image(new FileInputStream("prefrences src"))));
           * listView.getItems().add(Preferences);
           */
   //     } catch (Exception ex){
   //         Logger.getLogger(NaviBarController.class.getName()).log(null, (Supplier<String>) ex);
        
   //     }
   // }    
    
}
